<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.finalproject.demos.mapper.OrderItemMapper">
    <resultMap id="BaseResultMap" type="org.example.finalproject.demos.pojo.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="product_image" property="productImage"/>
        <result column="price" property="price"/>
        <result column="quantity" property="quantity"/>
        <result column="subtotal" property="subtotal"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insertBatch" parameterType="list">
        INSERT INTO order_items (
            order_id, product_id, product_name, product_image, price, quantity, subtotal, create_time
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.orderId}, #{item.productId}, #{item.productName}, #{item.productImage},
                #{item.price}, #{item.quantity}, #{item.subtotal}, #{item.createTime}
            )
        </foreach>
    </insert>

    <select id="selectByOrderId" parameterType="int" resultMap="BaseResultMap">
        SELECT * FROM order_items WHERE order_id = #{orderId} ORDER BY id
    </select>

    <select id="selectById" parameterType="int" resultMap="BaseResultMap">
        SELECT * FROM order_items WHERE id = #{id}
    </select>
</mapper>
